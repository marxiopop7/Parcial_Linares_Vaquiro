<?xml version="1.0" encoding="UTF-8"?>
<web-app xmlns="https://jakarta.ee/xml/ns/jakartaee"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="https://jakarta.ee/xml/ns/jakartaee https://jakarta.ee/xml/ns/jakartaee/web-app_6_0.xsd"
         version="6.0">

    <display-name>taller1</display-name>

    <!-- Modo desarrollo: JSF deja de cachear los recursos de /resources
         (en Production los sirve con Cache-Control de 7 dias, y los cambios
         en estilos.css no se ven hasta vaciar la cache del navegador). -->
    <context-param>
        <param-name>jakarta.faces.PROJECT_STAGE</param-name>
        <param-value>Development</param-value>
    </context-param>

    <!-- Sin esto, los recursos de /resources se sirven con Cache-Control
         de 7 dias y hay que vaciar la cache del navegador en cada cambio.
         Sube el valor (en milisegundos) cuando pases a produccion. -->
    <context-param>
        <param-name>com.sun.faces.defaultResourceMaxAge</param-name>
        <param-value>0</param-value>
    </context-param>

    <!-- Página de inicio predeterminada -->
    <welcome-file-list>
        <welcome-file>index.xhtml</welcome-file>
    </welcome-file-list>

    <!-- Configuración del Servlet de JSF (FacesServlet) -->
    <servlet>
        <servlet-name>Faces Servlet</servlet-name>
        <servlet-class>jakarta.faces.webapp.FacesServlet</servlet-class>
        <load-on-startup>1</load-on-startup>
    </servlet>
    
    <!-- Mapeo para que todas las peticiones .xhtml pasen por JSF -->
    <servlet-mapping>
        <servlet-name>Faces Servlet</servlet-name>
        <url-pattern>*.xhtml</url-pattern>
    </servlet-mapping>

</web-app>